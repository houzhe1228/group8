name: recipe_meal_planning_app
description: A new Flutter project for recipe and meal planning.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^0.13.3
  flutter_html: ^3.0.0-beta.2
  cached_network_image: ^3.1.0
  flutter_svg: ^2.0.0+1
  html: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
